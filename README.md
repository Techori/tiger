# tiger

tiger/
├── assets/           # App icons aur images ke liye
├── src/              # Saara main logic yahan rahega
│   ├── components/   # Chote UI elements (Buttons, Cards)
│   ├── screens/      # Web Dashboard aur App ki main screens
│   ├── services/     # SMS Listener aur API (Render) calls ke liye logic
│   ├── hooks/        # Permissions aur Location ke liye custom hooks
│   └── utils/        # Helpers jaise formatting dates ya device info
├── app.json          # Android permissions aur package configuration
├── App.js            # Main entry point
└── package.json      # Installed libraries ki list
